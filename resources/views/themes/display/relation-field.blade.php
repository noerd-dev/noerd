{{-- A relation as text: the resolved title, nothing to pick or clear. --}}
<div class="flex gap-2 text-sm">
    <div class="w-28 shrink-0 truncate text-gray-500" title="{{ __($label) }}">{{ __($label) }}</div>
    <div class="min-w-0 truncate text-gray-900">{{ $displayTitle }}</div>
</div>
