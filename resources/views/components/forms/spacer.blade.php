{{-- Layout spacer: intentionally renders no control. The detail block still wraps it in a
     `col-span-*` cell, reserving an empty column so the following fields keep their position
     instead of moving up into the freed grid slot. --}}
