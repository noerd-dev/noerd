<?php

use Illuminate\Support\Facades\Route;

Route::group(['middleware' => ['noerd', 'app-access:{{module-name}}']], function (): void {
    Route::livewire('{{module-name}}', '{{module-name}}::{{module-name}}-dashboard')->name('{{module-name}}');
});
